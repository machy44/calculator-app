import styled from "@emotion/styled";
import { border, color } from "styled-system";
import { Input as SInput} from 'semantic-ui-react'


const Input = styled(SInput)(border, color);

export default Input;